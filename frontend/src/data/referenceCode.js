export const REFERENCE_CODE = {
  "array": {
    "kadane": "def kadane(arr):\n    current_sum = arr[0]\n    max_sum = arr[0]\n    for i in range(1, len(arr)):\n        current_sum = max(arr[i], current_sum + arr[i])\n        max_sum = max(max_sum, current_sum)\n    return max_sum",
    "moore_voting": "def moore_voting(arr):\n    candidate = None\n    count = 0\n    for num in arr:\n        if count == 0:\n            candidate = num\n        count += 1 if num == candidate else -1\n    return candidate",
    "binary_search": "def binary_search(arr, target):\n    low, high = 0, len(arr) - 1\n    while low <= high:\n        mid = (low + high) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            low = mid + 1\n        else:\n            high = mid - 1\n    return -1",
    "bubble_sort": "def bubble_sort(arr):\n    n = len(arr)\n    for i in range(n):\n        for j in range(0, n - i - 1):\n            if arr[j] > arr[j + 1]:\n                arr[j], arr[j + 1] = arr[j + 1], arr[j]\n    return arr",
    "selection_sort": "def selection_sort(arr):\n    n = len(arr)\n    for i in range(n):\n        min_idx = i\n        for j in range(i + 1, n):\n            if arr[j] < arr[min_idx]:\n                min_idx = j\n        arr[i], arr[min_idx] = arr[min_idx], arr[i]\n    return arr",
    "insertion_sort": "def insertion_sort(arr):\n    for i in range(1, len(arr)):\n        key = arr[i]\n        j = i - 1\n        while j >= 0 and arr[j] > key:\n            arr[j + 1] = arr[j]\n            j -= 1\n        arr[j + 1] = key\n    return arr",
    "merge_sort": "def merge_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    mid = len(arr) // 2\n    left = merge_sort(arr[:mid])\n    right = merge_sort(arr[mid:])\n    result = []\n    i = j = 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            result.append(left[i]); i += 1\n        else:\n            result.append(right[j]); j += 1\n    result.extend(left[i:])\n    result.extend(right[j:])\n    return result",
    "quick_sort": "def quick_sort(arr, low=0, high=None):\n    if high is None:\n        high = len(arr) - 1\n    if low < high:\n        pivot = arr[high]\n        i = low - 1\n        for j in range(low, high):\n            if arr[j] <= pivot:\n                i += 1\n                arr[i], arr[j] = arr[j], arr[i]\n        arr[i + 1], arr[high] = arr[high], arr[i + 1]\n        pi = i + 1\n        quick_sort(arr, low, pi - 1)\n        quick_sort(arr, pi + 1, high)\n    return arr",
    "prefix_sum": "def prefix_sum(arr):\n    prefix = [0] * len(arr)\n    prefix[0] = arr[0]\n    for i in range(1, len(arr)):\n        prefix[i] = prefix[i - 1] + arr[i]\n    return prefix",
    "sliding_window_max_sum": "def sliding_window_max_sum(arr, k):\n    window_sum = sum(arr[:k])\n    max_sum = window_sum\n    for i in range(k, len(arr)):\n        window_sum += arr[i] - arr[i - k]\n        max_sum = max(max_sum, window_sum)\n    return max_sum",
    "two_pointer_pair_sum": "def two_pointer_pair_sum(arr, target):\n    arr = sorted(arr)\n    left, right = 0, len(arr) - 1\n    while left < right:\n        s = arr[left] + arr[right]\n        if s == target:\n            return (arr[left], arr[right])\n        elif s < target:\n            left += 1\n        else:\n            right -= 1\n    return None",
    "dutch_national_flag": "def dutch_national_flag(arr):\n    low, mid, high = 0, 0, len(arr) - 1\n    while mid <= high:\n        if arr[mid] == 0:\n            arr[low], arr[mid] = arr[mid], arr[low]\n            low += 1; mid += 1\n        elif arr[mid] == 1:\n            mid += 1\n        else:\n            arr[mid], arr[high] = arr[high], arr[mid]\n            high -= 1\n    return arr",
    "heap_sort": "def heap_sort(arr):\n    n = len(arr)\n\n    def heapify(n, i):\n        largest = i\n        l, r = 2 * i + 1, 2 * i + 2\n        if l < n and arr[l] > arr[largest]:\n            largest = l\n        if r < n and arr[r] > arr[largest]:\n            largest = r\n        if largest != i:\n            arr[i], arr[largest] = arr[largest], arr[i]\n            heapify(n, largest)\n\n    for i in range(n // 2 - 1, -1, -1):\n        heapify(n, i)\n    for i in range(n - 1, 0, -1):\n        arr[0], arr[i] = arr[i], arr[0]\n        heapify(i, 0)\n    return arr"
  },
  "recursion": {
    "factorial": "def factorial(n):\n    if n <= 1:\n        return 1\n    return n * factorial(n - 1)",
    "fibonacci": "def fibonacci(n):\n    if n <= 1:\n        return n\n    return fibonacci(n - 1) + fibonacci(n - 2)"
  },
  "dp": {
    "lcs": "def lcs(s1, s2):\n    \"\"\"Longest Common Subsequence. Returns (length, dp_table).\"\"\"\n    m, n = len(s1), len(s2)\n    dp = [[0] * (n + 1) for _ in range(m + 1)]\n    for i in range(1, m + 1):\n        for j in range(1, n + 1):\n            if s1[i - 1] == s2[j - 1]:\n                dp[i][j] = dp[i - 1][j - 1] + 1\n            else:\n                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])\n    return dp[m][n], dp",
    "knapsack": "def knapsack(weights, values, capacity):\n    n = len(weights)\n    dp = [[0] * (capacity + 1) for _ in range(n + 1)]\n    for i in range(1, n + 1):\n        for w in range(capacity + 1):\n            if weights[i - 1] <= w:\n                dp[i][w] = max(values[i - 1] + dp[i - 1][w - weights[i - 1]], dp[i - 1][w])\n            else:\n                dp[i][w] = dp[i - 1][w]\n    return dp[n][capacity], dp"
  },
  "graph": {
    "bfs": "def bfs(nodes, edges, start):\n    adj = build_adjacency(nodes, edges)\n    visited = set([start])\n    queue = [start]\n    steps = []\n    order = []\n\n    steps.append({\"step\": 0, \"current\": None, \"visited\": list(visited), \"queue\": list(queue), \"order\": list(order)})\n\n    while queue:\n        current = queue.pop(0)\n        order.append(current)\n        steps.append({\"step\": len(steps), \"current\": current, \"visited\": list(visited), \"queue\": list(queue), \"order\": list(order)})\n\n        for neighbor, _ in adj[current]:\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append(neighbor)\n                steps.append({\"step\": len(steps), \"current\": current, \"visited\": list(visited), \"queue\": list(queue), \"order\": list(order), \"note\": f\"discovered {neighbor}\"})\n\n    return steps, order",
    "dfs": "def dfs(nodes, edges, start):\n    adj = build_adjacency(nodes, edges)\n    visited = set()\n    steps = []\n    order = []\n\n    def visit(node):\n        visited.add(node)\n        order.append(node)\n        steps.append({\"step\": len(steps), \"current\": node, \"visited\": list(visited), \"order\": list(order)})\n        for neighbor, _ in adj[node]:\n            if neighbor not in visited:\n                visit(neighbor)\n\n    visit(start)\n    return steps, order",
    "dijkstra": "def dijkstra(nodes, edges, start):\n    adj = build_adjacency(nodes, edges, directed=True)\n    dist = {n: float(\"inf\") for n in nodes}\n    dist[start] = 0\n    visited = set()\n    pq = [(0, start)]\n    steps = []\n\n    steps.append({\"step\": 0, \"current\": None, \"distances\": dict(dist), \"visited\": list(visited)})\n\n    while pq:\n        d, node = heapq.heappop(pq)\n        if node in visited:\n            continue\n        visited.add(node)\n        steps.append({\"step\": len(steps), \"current\": node, \"distances\": {k: (v if v != float(\"inf\") else None) for k, v in dist.items()}, \"visited\": list(visited)})\n\n        for neighbor, weight in adj[node]:\n            if dist[node] + weight < dist[neighbor]:\n                dist[neighbor] = dist[node] + weight\n                heapq.heappush(pq, (dist[neighbor], neighbor))\n                steps.append({\n                    \"step\": len(steps), \"current\": node, \"relaxed\": neighbor,\n                    \"distances\": {k: (v if v != float(\"inf\") else None) for k, v in dist.items()},\n                    \"visited\": list(visited),\n                })\n\n    return steps, {k: (v if v != float(\"inf\") else None) for k, v in dist.items()}",
    "prim": "def prim(nodes, edges):\n    adj = build_adjacency(nodes, edges)\n    start = nodes[0]\n    visited = {start}\n    mst_edges = []\n    pq = [(w, start, v) for v, w in adj[start]]\n    heapq.heapify(pq)\n    steps = []\n\n    steps.append({\"step\": 0, \"visited\": list(visited), \"mst_edges\": list(mst_edges)})\n\n    while pq and len(visited) < len(nodes):\n        weight, u, v = heapq.heappop(pq)\n        if v in visited:\n            continue\n        visited.add(v)\n        mst_edges.append([u, v, weight])\n        steps.append({\"step\": len(steps), \"current\": v, \"visited\": list(visited), \"mst_edges\": list(mst_edges)})\n        for neighbor, w in adj[v]:\n            if neighbor not in visited:\n                heapq.heappush(pq, (w, v, neighbor))\n\n    return steps, mst_edges",
    "kruskal": "def kruskal(nodes, edges):\n    parent = {n: n for n in nodes}\n\n    def find(x):\n        while parent[x] != x:\n            x = parent[x]\n        return x\n\n    def union(x, y):\n        parent[find(x)] = find(y)\n\n    sorted_edges = sorted(edges, key=lambda e: e[2] if len(e) > 2 else 1)\n    mst_edges = []\n    steps = []\n    steps.append({\"step\": 0, \"mst_edges\": list(mst_edges), \"considering\": None})\n\n    for u, v, w in sorted_edges:\n        root_u, root_v = find(u), find(v)\n        accepted = root_u != root_v\n        if accepted:\n            union(u, v)\n            mst_edges.append([u, v, w])\n        steps.append({\n            \"step\": len(steps), \"considering\": [u, v, w],\n            \"accepted\": accepted, \"mst_edges\": list(mst_edges),\n        })\n\n    return steps, mst_edges",
    "bellman_ford": "def bellman_ford(nodes, edges, start):\n    dist = {n: float(\"inf\") for n in nodes}\n    dist[start] = 0\n    steps = []\n    steps.append({\"step\": 0, \"distances\": dict(dist), \"relaxed_edge\": None})\n\n    for i in range(len(nodes) - 1):\n        for u, v, w in edges:\n            if dist[u] != float(\"inf\") and dist[u] + w < dist[v]:\n                dist[v] = dist[u] + w\n                steps.append({\n                    \"step\": len(steps), \"iteration\": i + 1,\n                    \"relaxed_edge\": [u, v, w],\n                    \"distances\": {k: (val if val != float(\"inf\") else None) for k, val in dist.items()},\n                })\n\n    return steps, {k: (v if v != float(\"inf\") else None) for k, v in dist.items()}",
    "floyd_warshall": "def floyd_warshall(nodes, edges):\n    dist = {u: {v: float(\"inf\") for v in nodes} for u in nodes}\n    for n in nodes:\n        dist[n][n] = 0\n    for u, v, w in edges:\n        dist[u][v] = w\n\n    steps = []\n    for k in nodes:\n        for i in nodes:\n            for j in nodes:\n                if dist[i][k] + dist[k][j] < dist[i][j]:\n                    dist[i][j] = dist[i][k] + dist[k][j]\n                    steps.append({\n                        \"step\": len(steps), \"via\": k, \"from\": i, \"to\": j,\n                        \"new_distance\": dist[i][j],\n                    })\n\n    clean_dist = {i: {j: (v if v != float(\"inf\") else None) for j, v in row.items()} for i, row in dist.items()}\n    return steps, clean_dist",
    "topological_sort": "def topological_sort(nodes, edges):\n    adj = build_adjacency(nodes, edges, directed=True)\n    visited = set()\n    stack = []\n    steps = []\n\n    def visit(node):\n        visited.add(node)\n        steps.append({\"step\": len(steps), \"current\": node, \"visited\": list(visited), \"stack\": list(stack)})\n        for neighbor, _ in adj[node]:\n            if neighbor not in visited:\n                visit(neighbor)\n        stack.append(node)\n        steps.append({\"step\": len(steps), \"finished\": node, \"visited\": list(visited), \"stack\": list(stack)})\n\n    for n in nodes:\n        if n not in visited:\n            visit(n)\n\n    return steps, list(reversed(stack))"
  },
  "tree": {
    "bst_insert": "def bst_insert_sequence(values):\n    root = None\n    steps = []\n\n    def insert(node, value):\n        if node is None:\n            return BSTNode(value)\n        if value < node.value:\n            node.left = insert(node.left, value)\n        else:\n            node.right = insert(node.right, value)\n        return node\n\n    for v in values:\n        root = insert(root, v)\n        steps.append({\n            \"step\": len(steps),\n            \"inserted\": v,\n            \"tree\": root.to_dict() if root else None,\n        })\n\n    return steps, root.to_dict() if root else None",
    "avl_insert": "def avl_insert_sequence(values):\n    root = None\n    steps = []\n\n    def insert(node, value):\n        if node is None:\n            return AVLNode(value)\n        if value < node.value:\n            node.left = insert(node.left, value)\n        else:\n            node.right = insert(node.right, value)\n\n        node.height = 1 + max(_height(node.left), _height(node.right))\n        balance = _balance(node)\n\n        if balance > 1 and value < node.left.value:\n            return _rotate_right(node)\n        if balance < -1 and value > node.right.value:\n            return _rotate_left(node)\n        if balance > 1 and value > node.left.value:\n            node.left = _rotate_left(node.left)\n            return _rotate_right(node)\n        if balance < -1 and value < node.right.value:\n            node.right = _rotate_right(node.right)\n            return _rotate_left(node)\n\n        return node\n\n    for v in values:\n        root = insert(root, v)\n        steps.append({\n            \"step\": len(steps),\n            \"inserted\": v,\n            \"tree\": root.to_dict() if root else None,\n        })\n\n    return steps, root.to_dict() if root else None",
    "heap_insert": "def heap_insert_sequence(values):\n    heap = []\n    steps = []\n\n    def sift_up(i):\n        while i > 0:\n            parent = (i - 1) // 2\n            if heap[i] < heap[parent]:\n                heap[i], heap[parent] = heap[parent], heap[i]\n                i = parent\n            else:\n                break\n\n    for v in values:\n        heap.append(v)\n        sift_up(len(heap) - 1)\n        steps.append({\"step\": len(steps), \"inserted\": v, \"heap_array\": list(heap)})\n\n    return steps, list(heap)",
    "segment_tree": "def build_segment_tree(arr):\n    n = len(arr)\n    tree = [0] * (2 * n)\n    steps = []\n\n    for i in range(n):\n        tree[n + i] = arr[i]\n    for i in range(n - 1, 0, -1):\n        tree[i] = tree[2 * i] + tree[2 * i + 1]\n        steps.append({\"step\": len(steps), \"node_index\": i, \"value\": tree[i], \"tree_array\": list(tree)})\n\n    return steps, tree",
    "fenwick_tree": "def build_fenwick_tree(arr):\n    n = len(arr)\n    tree = [0] * (n + 1)\n    steps = []\n\n    for i, val in enumerate(arr, start=1):\n        idx = i\n        while idx <= n:\n            tree[idx] += val\n            steps.append({\"step\": len(steps), \"updated_index\": idx, \"value\": tree[idx], \"tree_array\": list(tree)})\n            idx += idx & (-idx)\n\n    return steps, tree",
    "trie_insert": "def trie_insert_sequence(words):\n    trie = {}\n    steps = []\n\n    for word in words:\n        node = trie\n        for ch in word:\n            if ch not in node:\n                node[ch] = {}\n            node = node[ch]\n        node[\"$end\"] = True\n        steps.append({\"step\": len(steps), \"inserted_word\": word, \"trie\": _trie_copy(trie)})\n\n    return steps, trie"
  }
};
